const library = {
    books: [],
    card: [],
    cardOperations(){},
    toggleReadStatus(bookId){},
    status(bookId){},
};

function Book(title, author, pages, read){}

function Card(bookId, title, author, pages, read){}

const formInputs = {}

function addBookToLibrary(event){}

function renderBook(book){}

function clearForm(){}

// const form
    // add event listener



